services:
  web:
    build: .
    container_name: jobportal
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - ./.env
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.django.development